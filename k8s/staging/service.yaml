apiVersion: v1
kind: Service
metadata:
  name: flask-example-service
  namespace: staging
spec:
  selector:
    app: flask-example
  ports:
    - protocol: TCP
      port: 80
      targetPort: 5000
  type: NodePort  # or ClusterIP, depending on your cluster access
